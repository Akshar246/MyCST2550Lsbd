#include <iostream>
int main() {

  double x, y;
  
  std::cout << "Enter two numbers: ";
  std::cin  >> x >> y;

  double sum =  x + y;
  double difference  = x - y;
  double product = x * y;
  double distance = y - x;
  double average = sum  / 2;

  std::cout << "The sum of " << x << " and " <<  y << " is: " << sum << std::endl;
  std::cout << "The difference of " << x << " and " << y << " is: " << difference  << std::endl;

  std::cout << "The product of " << x << " And " << y << " is: " << product << std::endl;
  std::cout << "The average of " << x << " and " << y << " is: " << average << std::endl;
  std::cout << "The distence between " << x << " and " << y << " is: " << distance << std::endl;
  

  return 0;
  return 0;
  return 0;
  return 0;
  return 0;

}
