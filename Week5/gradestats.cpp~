#include <iostream>
int main ()
{
  int grade = 0;
  int i;

  std::cout << "Enter Grade: (or -1 to end): ";
  std::cout >> grade >> i;
  for ( i = 0 && i <= 100 ) {
  }
  if (i = -1)
    break;

      grade[i] = grade;
        sum += grade;
        i++;
    }
